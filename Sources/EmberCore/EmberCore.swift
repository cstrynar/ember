// EmberCore — platform-agnostic domain logic for Ember (personal health coach).
// No UIKit, no SwiftUI, no networking. Pure, deterministic, `swift test`-able.
// Network + AI live only in the App layer (the coach calls the Claude API there).
// All public types are defined in their individual source files.
