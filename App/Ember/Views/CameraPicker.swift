import SwiftUI
import UIKit

/// A minimal SwiftUI wrapper over `UIImagePickerController` configured for the camera. Hands the
/// captured `UIImage` back via `onImage` and dismisses itself on capture or cancel. Presented from
/// `PhotoEstimateView` only when `UIImagePickerController.isSourceTypeAvailable(.camera)` is true.
struct CameraPicker: UIViewControllerRepresentable {
    /// Called with the captured photo (the picker's `.originalImage`).
    var onImage: (UIImage) -> Void
    @Environment(\.dismiss) private var dismiss

    func makeUIViewController(context: Context) -> UIImagePickerController {
        let picker = UIImagePickerController()
        picker.sourceType = .camera
        picker.delegate = context.coordinator
        return picker
    }

    func updateUIViewController(_ uiViewController: UIImagePickerController, context: Context) {}

    func makeCoordinator() -> Coordinator { Coordinator(self) }

    final class Coordinator: NSObject, UIImagePickerControllerDelegate, UINavigationControllerDelegate {
        private let parent: CameraPicker

        init(_ parent: CameraPicker) { self.parent = parent }

        func imagePickerController(_ picker: UIImagePickerController,
                                   didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
            if let image = info[.originalImage] as? UIImage {
                parent.onImage(image)
            }
            parent.dismiss()
        }

        func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
            parent.dismiss()
        }
    }
}
